const whiskyGlass = document.body.getElementsByClassName('whisky').item(0);

window.addEventListener('load', (whiskyGlass) => {
  // TODO : Displaying the empty whisky glass when you click on the glass.
});
